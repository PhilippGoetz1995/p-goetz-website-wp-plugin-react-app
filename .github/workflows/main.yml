on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: Use Node.js 16
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      - name: 🔨 Build Project
        run: |
          npm install
          npm run build

      - name: 📂 Sync files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ssh.strato.de
          port: 22
          username: ${{ secrets.STRATO_SFTP_USER }}
          key: ${{ secrets.STRATO_SFTP_PWD }}
          source: ./build/
          target: /var/www/html
